<h1>Movies</h1>

<ul>
  <% @movies.each do |movie| %>
    <li><%= link_to "#{movie.title} (#{movie.length_in_minutes}m)", movie %></li>
  <% end %>
</ul>

<% if manager? %>
  <%= link_to "Add new movie", new_movie_path %>
<% end %>
